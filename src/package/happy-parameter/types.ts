export interface pageContentType {
    pageNumber?: number,
    pageSize?: number,
    pageSizes?: number[],
    totalRecord?: number,
    small?: Boolean,
    background?: Boolean,
    disabled?: Boolean,
    layout?: string,
}